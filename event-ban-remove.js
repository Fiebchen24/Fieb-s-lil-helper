const { SlashCommandBuilder, PermissionFlagsBits } = require('discord.js');
const banService=require('../services/banService'); const config=require('../config');
module.exports={data:new SlashCommandBuilder().setName('event-ban-remove').setDescription('Remove an event ban.').addUserOption(o=>o.setName('player').setDescription('Player').setRequired(true)).setDefaultMemberPermissions(PermissionFlagsBits.ManageGuild),async execute(interaction){const user=interaction.options.getUser('player',true); const c=banService.removeBan(user.id); const member=await interaction.guild.members.fetch(user.id).catch(()=>null); if(member&&config.roles.eventBan) await member.roles.remove(config.roles.eventBan).catch(()=>{}); return interaction.reply({content:`Removed ${c} active event ban(s) from ${user.username}.`,ephemeral:true});}};
