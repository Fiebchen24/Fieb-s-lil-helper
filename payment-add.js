const { SlashCommandBuilder, PermissionFlagsBits } = require('discord.js');
const paymentService=require('../services/paymentService'); const { paymentLog }=require('../services/logService');
module.exports={data:new SlashCommandBuilder().setName('payment-add').setDescription('Add an open prize/payment ticket entry.').addUserOption(o=>o.setName('player').setDescription('Player').setRequired(true)).addNumberOption(o=>o.setName('amount').setDescription('Amount').setRequired(true).setMinValue(0.01)).addStringOption(o=>o.setName('currency').setDescription('Currency').setRequired(true).addChoices({name:'USD',value:'USD'},{name:'EUR',value:'EUR'})).addStringOption(o=>o.setName('event').setDescription('Event name').setRequired(true)).addStringOption(o=>o.setName('method').setDescription('PayPal/Revolut/etc')).addStringOption(o=>o.setName('note').setDescription('Note')).setDefaultMemberPermissions(PermissionFlagsBits.ManageGuild),async execute(interaction,client){const user=interaction.options.getUser('player',true); const p=paymentService.addPayment({user,amount:interaction.options.getNumber('amount',true),currency:interaction.options.getString('currency',true),eventName:interaction.options.getString('event',true),method:interaction.options.getString('method')||'PayPal',note:interaction.options.getString('note')||'',addedBy:interaction.user}); await paymentLog(client,`💸 Payment opened: #${p.id} <@${user.id}> **${p.amount} ${p.currency}** for **${p.event_name}**.`); return interaction.reply({content:`Payment #${p.id} added for <@${user.id}>.`,ephemeral:true});}};
