const { SlashCommandBuilder, AttachmentBuilder, PermissionFlagsBits } = require('discord.js');
const paymentService=require('../services/paymentService');
module.exports={data:new SlashCommandBuilder().setName('payment-export').setDescription('Export payments as CSV.').addStringOption(o=>o.setName('status').setDescription('Status').addChoices({name:'Open',value:'open'},{name:'Paid',value:'paid'},{name:'All',value:'all'})).setDefaultMemberPermissions(PermissionFlagsBits.ManageGuild),async execute(interaction){const status=interaction.options.getString('status')||'all'; const csv=paymentService.exportCsv(status); const file=new AttachmentBuilder(Buffer.from(csv,'utf8'),{name:`payments-${status}.csv`}); return interaction.reply({content:'Payment export ready.',files:[file],ephemeral:true});}};
