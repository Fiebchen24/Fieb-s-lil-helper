const { SlashCommandBuilder, EmbedBuilder, PermissionFlagsBits } = require('discord.js');
const paymentService=require('../services/paymentService');
module.exports={data:new SlashCommandBuilder().setName('payment-list').setDescription('List prize/payment entries.').addStringOption(o=>o.setName('status').setDescription('Status').addChoices({name:'Open',value:'open'},{name:'Paid',value:'paid'},{name:'All',value:'all'})).setDefaultMemberPermissions(PermissionFlagsBits.ManageGuild),async execute(interaction){const status=interaction.options.getString('status')||'open'; const rows=paymentService.list(status).slice(0,25); if(!rows.length) return interaction.reply({content:`No ${status} payments.`,ephemeral:true}); const e=new EmbedBuilder().setTitle(`Payments • ${status}`).setDescription(rows.map(p=>`#${p.id} • <@${p.discord_id}> • **${p.amount} ${p.currency}** • ${p.status} • ${p.event_name}`).join('\n')); return interaction.reply({embeds:[e],ephemeral:true});}};
