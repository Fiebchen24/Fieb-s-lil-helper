const { SlashCommandBuilder, PermissionFlagsBits } = require('discord.js');
const paymentService=require('../services/paymentService'); const { paymentLog }=require('../services/logService');
module.exports={data:new SlashCommandBuilder().setName('payment-paid').setDescription('Mark a payment as paid.').addIntegerOption(o=>o.setName('payment_id').setDescription('Payment ID').setRequired(true)).setDefaultMemberPermissions(PermissionFlagsBits.ManageGuild),async execute(interaction,client){const p=paymentService.markPaid(interaction.options.getInteger('payment_id',true), interaction.user); if(!p) return interaction.reply({content:'Payment not found.',ephemeral:true}); await paymentLog(client,`✅ Payment paid: #${p.id} <@${p.discord_id}> **${p.amount} ${p.currency}** by <@${interaction.user.id}>.`); return interaction.reply({content:`Payment #${p.id} marked as paid.`,ephemeral:true});}};
